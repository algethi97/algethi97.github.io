# 🍊 만다린 게임 (Mandarin Game) 웹게임 구현 계획서 (수정안)

본 문서는 사과게임(Fruit Box)을 모티브로 하여, 홈페이지의 **데스크테리어 & 웜 미니멀리즘(Warm Minimalist Aesthetic)** 컨셉에 맞춰 만다린 귤 에셋(`images/mandarin.png`)을 활용한 웹 퍼즐 게임을 제작하고, **Pop 효과음(SFX)** 및 **사이드 Top 5 랭킹 시스템**, Supabase 데이터베이스 연동을 구축하기 위한 수정 구현 계획서입니다.

---

## 1. 프로젝트 개요 및 변경 사항

- **모티브 게임**: [Fruit Box (사과게임)](https://www.gamesaien.com/game/fruit_box_a/)
- **게임 타이틀**: 만다린 게임 (Mandarin Puzzle)
- **비주얼 에셋**: `images/mandarin.png` (기존 프로젝트 귤 일러스트 에셋)
- **테마 & 톤앤매너**:
  - 사이트 고유의 감성 컬러: 오렌지 포인트(`--accent-orange`: `#ea580c`), 우드 포인트(`--accent-wood`: `#78350f`), 웜 베이지(`--bg-main`: `#f5f2eb`)
  - 데스크테리어 무드의 카드형 UI와 부드러운 애니메이션
- **반영된 신규 요구사항**:
  1. **Pop 효과음 시스템 (Web Audio API)**:
     - 귤 타일이 터질 때 톡 터지는 청량하고 통통 튀는 Pop 사운드 효과음
     - 외부 파일 다운로드 없는 순수 브라우저 오디오 신디사이징 (0ms 로딩 지연)
     - 상단 대시보드(HUD)에 🔊 사운드 On/Off 토글 버튼 제공 (`localStorage`에 설정 유지)
  2. **사이드 Top 5 랭킹 시스템 (Side Leaderboard)**:
     - 데스크톱 기준 **2-Column 레이아웃**: 좌측 메인 게임 보드 + 우측 사이드 Top 5 명예의 전당 카드
     - 모바일/태블릿: 세로 1-Column 반응형 자동 전환
     - 플레이 중 실시간으로 1~5위 목표 점수를 바로 옆에서 확인 가능
     - 게임 완료 후 점수 등록 시 사이드 Top 5가 즉각 실시간 리프레시되며 본인 순위 하이라이트

---

## 2. 화면 레이아웃 및 UI 구조

```
┌───────────────────────────────────────────────────────────────────────────┐
│ 🏠 홈 | 👤 자기소개 | 💼 포트폴리오 | 📝 방명록 | 🍊 게임                    │
├───────────────────────────────────────────────────────────────────────────┤
│                     🍊 만다린 게임 (Mandarin Puzzle)                      │
│                                                                           │
│ [ ⏱️ 120s  ======================= ]  [ 🏆 점수: 24 ]  [ 🔊/🔇 ] [ 🔄 ]   │
├────────────────────────────────────────┬──────────────────────────────────┤
│                                        │  🏆 명예의 전당 (TOP 5)          │
│  [ 메인 게임 보드 영역 ]               │ ──────────────────────────────── │
│                                        │  🥇 1st: 감귤대장   152점 (어제)  │
│  17열 × 10행 만다린 타일 그리드         │  🥈 2nd: 오렌지킹   138점 (3일전) │
│  - 마우스/터치 드래그 영역 선택         │  🥉 3rd: 제주귤     124점 (오늘)  │
│  - 합계 10 도달 시 톡! Pop 사운드      │  4th: 귤까기인형   110점          │
│  - 타일 팝 애니메이션 & 점수 플로팅    │  5th: 만다린러버    98점          │
│                                        │ ──────────────────────────────── │
│                                        │  💡 합계가 10이 되도록 드래그!   │
└────────────────────────────────────────┴──────────────────────────────────┘
```

---

## 3. 사운드 효과 (Web Audio API)

- **Pop 효과음 원리**:
  - 브라우저 내장 `AudioContext` 활용
  - 0.08초 동안 Sine 파형을 800Hz &rarr; 150Hz로 급격히 하강시키며 부드러운 엔벨로프(Envelope) 적용
  - 경쾌하고 상큼한 귤 터지는 팝핑 사운드 완성
- **사운드 컨트롤**:
  - `🔊 소리 켜짐` / `🔇 음소거` 토글 버튼 제공
  - 브라우저 정책(Autoplay Policy)을 준수하여 최초 사용자 인터랙션(클릭) 시 오디오 컨텍스트 자동 활성화

---

## 4. Supabase 데이터베이스 스키마 및 RLS

프로젝트 ID `fspkdczsrfoyebznfkwr` 내 `"mandarin-game_scores"` 테이블을 다음과 같이 확장합니다:

### 4.1 테이블 컬럼 정의

| 컬럼명 | 데이터 타입 | 제약 조건 | 기본값 | 설명 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY` | - | 기록 식별자 PK |
| `nickname` | `VARCHAR(30)` | `NOT NULL` | - | 플레이어 닉네임 (1~20자) |
| `score` | `INTEGER` | `NOT NULL` | - | 최종 달성 스코어 (0 ~ 170) |
| `remaining_count`| `INTEGER` | `NOT NULL` | `0` | 게임 종료 시 남은 귤 개수 |
| `play_time` | `INTEGER` | `NOT NULL` | `120` | 플레이 시간 (초 단위) |
| `created_at` | `TIMESTAMPTZ` | `NOT NULL` | `now()` | 달성 일시 (타임존 포함) |

### 4.2 RLS 보안 정책 및 Top 5 최적화
```sql
-- 1. RLS 활성화
ALTER TABLE "mandarin-game_scores" ENABLE ROW LEVEL SECURITY;

-- 2. 누구나 Top 5 조회 가능 (SELECT)
CREATE POLICY "Allow public select" 
ON "mandarin-game_scores" 
FOR SELECT 
TO anon, authenticated 
USING (true);

-- 3. 유효한 점수만 등록 가능 (INSERT)
CREATE POLICY "Allow public insert" 
ON "mandarin-game_scores" 
FOR INSERT 
TO anon, authenticated 
WITH CHECK (
  (char_length(trim(both from nickname)) >= 1) AND
  (char_length(trim(both from nickname)) <= 20) AND
  (score >= 0) AND (score <= 250)
);

-- 4. Top 5 쿼리 가속화 복합 인덱스
CREATE INDEX IF NOT EXISTS idx_mandarin_scores_top5 
ON "mandarin-game_scores" (score DESC, created_at ASC);
```

---

## 5. 단계별 실행 로드맵 (Roadmap)

> [!IMPORTANT]
> 본 문서는 계획서이며, 사용자의 명시적인 승인("진행해줘", "코드 작성해줘" 등) 후 각 단계가 실행됩니다.

1. **[Step 1] Supabase DB 스키마 완성**:
   - `"mandarin-game_scores"` 테이블 컬럼 추가 및 RLS 보안 정책 적용
   - Top 5 조회 인덱스 생성
2. **[Step 2] 사이드 랭킹 구조의 `game.html` 마크업 작성**:
   - 데스크테리어 2-Column 구조 (좌측: 게임 HUD + 17×10 보드, 우측: 실시간 Top 5 카드)
   - 음소거 토글 버튼, 게임오버 모달, Supabase CDN 연결
3. **[Step 3] `game.js` 코어 게임 엔진 개발**:
   - Web Audio API 기반 Pop 사운드 합성 엔진
   - 17×10 만다린 보드 생성, 마우스/터치 드래그 바운딩 박스 판정
   - 숫자 합계 10 도달 시 Pop 사운드 재생 & 타일 제거 애니메이션
   - 120초 타이머 및 Supabase 연동 (Top 5 실시간 조회 + 점수 저장)
4. **[Step 4] UI 스타일링 및 모바일 반응형 튜닝**:
   - 데스크톱 2열 &rarr; 모바일 1열 반응형 전환
   - 귤 타일 팝 애니메이션, Top 5 랭킹 메달 뱃지 스타일링
5. **[Step 5] 사이트 헤더 및 홈 화면 연동 & 검증**:
   - 전체 페이지 상단 탭 메뉴(`🍊 게임`) 및 홈 카드 추가
   - 크로스 브라우징 및 사운드/터치 동작 최종 점검
